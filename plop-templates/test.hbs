import { render, screen } from '@testing-library/react'; import Component from '../{{pascalCase
  name
}}'; test('renders
{{pascalCase name}}
component', () => { render(<Component />); expect(screen.getByText('{{pascalCase
  name
}}')).toBeInTheDocument(); });