{% comment %}
  Affichage du sous titre produit

  Accepte:
  product {object} - Objet Product Shopify
  subtitle_tag {string} - Le tag HTML utilisé pour le sous titre (default: 'span')
  subtitle_metafield {string} - Metafield au format 'namespace.key' (default: 'descriptors.subtitle')
  subtitle_class {string} - Classe css pour styliser le sous titre

  Usage:
  {% render 'f_render-product-subtitle' %}
{% endcomment %}

{% assign subtitle_tag = subtitle_tag | default: 'span' %}

{% assign subtitle_metafield_arr = subtitle_metafield | split: '.' %}
{% assign subtitle_value = product.metafields[subtitle_metafield_arr[0]][subtitle_metafield_arr[1]] %}

{% if subtitle_value %}
  <{{ subtitle_tag }} class="{{ subtitle_class }} heading heading--small">
    {{ subtitle_value }}
  </{{ subtitle_tag }}>
{% endif %}
