import styles from './UrichUtext.module.scss';

interface UrichUtextProps {}

export const UrichUtext = ({}: UrichUtextProps) => {
  return <div className={styles.UrichUtext}>UrichUtext works!</div>;
};
