
{{ 'alertmessage.css' | asset_url | stylesheet_tag }}

{%- style -%}
.alertmessage {
  padding: 20px;
}
{%- endstyle -%}
{%- liquid
  assign title = section.settings.title
-%}

<!-- AlertMessage -->
<div class="alertmessage section-{{ section.id }}">
  {{ title }}
</div>

{% schema %}
{
  "name": "AlertMessage",
  "settings": [
    {
      "type": "text",
      "id": "message",
      "label": "Message",
      "default": ""
    },
    {
      "type": "text",
      "id": "variant",
      "label": "Variant",
      "default": ""
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "AlertMessage",
      "category": "Custom",
      "blocks": []
    }
  ]
}
{% endschema %}
